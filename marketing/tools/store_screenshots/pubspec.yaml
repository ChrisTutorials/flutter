name: store_screenshots
description: Reusable tooling for deterministic store screenshot processing and validation.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.7.0
  image: ^4.5.4
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
