name: common_flutter_ads
description: A collection of reusable, high-value utilities for Flutter applications including ad service, platform detection, and navigation helpers
version: 1.0.0
homepage: https://github.com/example/common_flutter_ads

environment:
  sdk: ^3.11.1
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Core dependencies for ad service
  google_mobile_ads: ^7.0.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # No assets needed for this package
